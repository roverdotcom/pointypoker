// Re-export shim — definition moved to @v4/types/external during the v4 refactor.
// Kept so legacy code importing via @yappy/types keeps working.
export type {
  SprintRef,
  IssueTypeRef,
  ExternalReference,
} from '@v4/types/external';
